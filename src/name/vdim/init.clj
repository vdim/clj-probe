;; Counting some statistic from file with developer's working hours, e.g.
;; sum of hours and etc.

(ns name.vdim.init
  (:use name.vdim.whcount))

(get-stats "/home/adim/tsen/notes/wh-Dimitrov.txt")

